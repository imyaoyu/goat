{
	"SQL" :	"CREATE TABLE IF NOT EXISTS t_app_user (id INTEGER PRIMARY KEY,user_id TEXT NOT NULL UNIQUE,open_id TEXT NOT NULL UNIQUE,login_count INTEGER NOT NULL,created_time TEXT NOT NULL,updated_time TEXT NOT NULL);"
}